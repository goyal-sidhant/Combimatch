# Decisions Log: COMBIMATCH

> This file is auto-maintained by Claude Code. After EVERY message exchange,
> Claude Code appends a 2-3 line summary below. Do not edit manually unless
> correcting an error.

---

## Session 1 — 22-03-2026

### 22-03-2026 | Session 1 | Message 1
- **Discussed:** Full project kickoff. Read all 4 reference documents (CLAUDE.md, PROJECT_AUDIT, REBUILD_BRIEF, DECISIONS_LOG). Confirmed understanding of what the tool does, key differences between original and rebuild, recommended build order, and conflicts between audit and brief.
- **Decided/Built:** Created comprehensive build plan with 7 phases and sub-phases. 4 design decisions made: (1) Keep 3 tabs, (2) Indian number format (1,00,000), (3) Hybrid C solver (branch-and-bound for positives, exhaustive for negatives), (4) Session files in userdata/ subfolder.
- **Open:** Plan approved. Ready to start Sub-phase 1A.

### 22-03-2026 | Session 1 | Message 2
- **Discussed:** Three corrections to the plan: (1) N3 Quick Reset not listed in any phase, (2) Session auto-save should trigger after every finalization not just timer, (3) Single instance mutex must be per-session not per-machine for nComputing.
- **Decided/Built:** Updated plan with all 3 fixes. Added sub-phase breakdown for all phases. Added mandatory stop-and-confirm workflow.
- **Open:** One more fix: *.dll should be tracked in git (not gitignored).

### 22-03-2026 | Session 1 | Message 3
- **Discussed:** Plan finalized and approved after removing *.dll from .gitignore.
- **Decided/Built:** Sub-phase 1A complete — built 15 files: project skeleton (all folders, __init__.py files, .gitignore, requirements.txt), config/ (constants.py with all 20 colors and every named constant, settings.py with path helpers, mappings.py with color lookups), models/ (source_tag.py, number_item.py, combination.py, finalized_combination.py, search_parameters.py, session_state.py), core/ (number_parser.py, target_parser.py, parameter_validator.py), utils/ (format_helpers.py with Indian number formatting). All 8 test sections pass.
- **Open:** Awaiting user testing of Sub-phase 1A before proceeding to 1B (Solver Engine).

### 22-03-2026 | Session 2 | Message 1
- **Discussed:** User confirmed all Sub-phase 1A tests pass. Proceeded to Sub-phase 1B (Solver Engine).
- **Decided/Built:** Sub-phase 1B complete — built 3 files + 1 constant: (1) core/smart_bounds.py — compute_smart_bounds() with viable min/max via cumulative sum, search space estimation via math.comb(), no-solution detection, SEARCH_SPACE_WARNING_LIMIT constant (10 million). (2) core/solver_python.py — find_combinations() generator using itertools.combinations(), supports seed numbers (must-include items with adjusted target), search order (smallest/largest first), responsive stop flag checked every 100 iterations, progress callback, max_results limit. Handles negatives correctly. (3) core/solver_manager.py — SolverManager class (DLL detection, smart bounds computation, thread creation) + SolverThread (QThread with batched signal emission: results_batch, progress, search_complete, error_occurred). Test script test_phase1b.py with 15 test sections.
- **Open:** Awaiting user testing of Sub-phase 1B before proceeding to 1C (App Shell).

### 22-03-2026 | Session 2 | Message 2
- **Discussed:** User confirmed Sub-phase 1B tests pass (including stop flag fix). Proceeded to Sub-phase 1C (App Shell).
- **Decided/Built:** Sub-phase 1C complete — built 4 files: (1) gui/styles.py — full QSS stylesheet with soft blue-grey palette, 25+ named color constants, styles for tabs/buttons/inputs/lists/progress/scrollbars/tooltips. (2) utils/single_instance.py — Windows mutex via ctypes with Local\\ prefix (per-session, not per-machine) for nComputing compatibility. (3) gui/main_window.py — QMainWindow with QTabWidget (3 placeholder tabs: Find, Summary, Settings), centered on screen using QScreen (not deprecated QDesktopWidget), status bar. (4) main.py — entry point: single instance check, QApplication with stylesheet, MainWindow launch.
- **Open:** Awaiting user testing of Sub-phase 1C before proceeding to 1D (Input + Source Panels).

### 22-03-2026 | Session 2 | Message 3
- **Discussed:** User confirmed Sub-phase 1C tests pass. Proceeded to Sub-phase 1D (Input + Source Panels).
- **Decided/Built:** Sub-phase 1D complete — built 3 files: (1) gui/input_panel.py — InputPanel with input mode dropdown (line/semicolon), text area, Load button with status, search params group (target, buffer, min/max size, max results, search order dropdown), Find/Stop buttons, progress bar with label, bounds hint, Clear All button (N3). Emits signals: numbers_loaded, find_requested, stop_requested, clear_all_requested. (2) gui/source_panel.py — SourcePanel with source number list using Qt.UserRole for item index storage (prevents combo removal mismatch bug), orange QBrush highlighting for selected combo items, count/total header, embeds ComboInfoPanel. (3) gui/combo_info_panel.py — Shows selected combo details (sum, difference, count, values in Indian format).
- **Open:** Awaiting user testing of Sub-phase 1D before proceeding to 1E (Results Panel + Find Tab Assembly).

### 22-03-2026 | Session 2 | Message 4
- **Discussed:** User confirmed Sub-phase 1D tests pass. Proceeded to Sub-phase 1E (Results Panel + Find Tab Assembly).
- **Decided/Built:** Sub-phase 1E complete — built 2 new files + updated 1: (1) gui/results_panel.py — ResultsPanel with exact/approximate split lists, size-group headers with counts (collapsible via non-selectable header items), Combination stored in Qt.UserRole (not by index), remove_invalid_combinations() filters by item_indices intersection, combination_selected signal. (2) gui/find_tab.py — FindTab conductor wiring InputPanel, ResultsPanel, SourcePanel via signals. Full lifecycle: validate params → compute smart bounds → warn if no solution/huge space → start SolverThread → receive batches → display → highlight on select. (3) Updated gui/main_window.py — replaced Find placeholder with real FindTab. App is now fully functional for manual input searching.
- **Open:** Awaiting user testing of Sub-phase 1E (full search flow) before proceeding to Phase 2 (Finalization).

### 22-03-2026 | Session 2 | Message 5
- **Discussed:** User requested: (1) Move all test files to tests/ folder, add tests/ to .gitignore. (2) Bug fix: max_results was capping total results (exact + approximate combined), causing exact matches at larger sizes to be lost when approximate matches at smaller sizes filled the limit.
- **Decided/Built:** (1) Created tests/ folder, moved 5 test files, added tests/ to .gitignore, fixed sys.path in all moved files. (2) Changed solver_python.py — max_results now caps ONLY approximate matches. Exact matches are NEVER capped. Solver continues searching all sizes for exact matches even after approx cap is reached. Updated test_phase1b.py Test 11 for new semantics. Created tests/test_max_results_exact.py with 4 targeted tests including the key scenario: exact match at size 3 found despite approx cap reached at size 2. All 19 tests pass (15 regression + 4 new).
- **Open:** Awaiting user confirmation before proceeding to Phase 2.

### 22-03-2026 | Session 2 | Message 6
- **Discussed:** User confirmed Sub-phase 1E and max_results fix pass. Proceeded to Phase 2, Sub-phase 2A (Finalization Manager + Dialogs).
- **Decided/Built:** Sub-phase 2A complete — built 2 files: (1) core/finalization_manager.py — FinalizationManager class: finalize_combination() assigns next color from 20-color cycling palette, marks items via NumberItem.index (never list position), pushes LIFO undo record. undo_last() reverses marks, restores color index and combo number. Also: get_finalized_indices(), get_unmatched_items(), get/restore_state(), clear(). Single source of truth for all finalization state. (2) gui/dialogs.py — 9 dialog functions: ask_label (text input for combo label), confirm_clear_all (with stronger warning if finalized combos exist), confirm_close_unsaved (save/discard/cancel), warn_no_solution, warn_large_search_space, warn_excel_closed, show_error, show_info, confirm_action. Test script tests/test_phase2a.py with 9 tests: basic finalization, color cycling (wraps at 21), undo (LIFO, restores color index), finalized indices, unmatched items, clear, state save/restore, dialogs import.
- **Open:** Awaiting user testing of Sub-phase 2A before proceeding to 2B (Summary Tab + Wiring Finalization into UI).

### 22-03-2026 | Session 2 | Message 7
- **Discussed:** User confirmed Sub-phase 2A tests pass. Proceeded to Sub-phase 2B (Summary Tab + Wiring Finalization into UI).
- **Decided/Built:** Sub-phase 2B complete — built 1 new file + updated 4: (1) gui/summary_tab.py (NEW) — SummaryTab with scrollable colored finalization cards (color-tinted background, combo number, label, sum, difference, values), stats bar (combo count, items matched, total value, exact/approx breakdown), Undo button with undo_requested signal, refresh() rebuilds all cards, clear() resets. (2) gui/results_panel.py (UPDATED) — Added Finalize button at bottom of panel, finalize_requested signal, button enables/disables with combo selection. (3) gui/find_tab.py (UPDATED) — Added FinalizationManager instance, finalization_changed signal, _on_finalize_requested() handler (gets selected combo → ask_label dialog → finalize → remove invalid combos → refresh source → emit signal), undo_last_finalization() method, get_finalization_manager() accessor. Clear All now uses confirm_clear_all dialog and clears finalization state. (4) gui/main_window.py (UPDATED) — Replaced Summary placeholder with real SummaryTab, wired finalization_changed → _on_finalization_changed (refreshes Summary), wired undo_requested → _on_undo_requested (forwards to FindTab). (5) gui/source_panel.py (UPDATED) — Added refresh_display() method for post-finalization UI rebuild. Test script tests/test_phase2b.py with 12 tests: Finalize button exists/enables/disables, source panel refresh, summary tab refresh/undo signal/clear, FindTab has manager/signal, MainWindow uses real SummaryTab, full finalization flow (finalize → remove invalid → undo), results removal correctness. All 12 tests pass + 9 regression tests pass.
- **Open:** Awaiting user testing of Sub-phase 2B before proceeding to Phase 3 (Excel Integration).

### 22-03-2026 | Session 2 | Message 8
- **Discussed:** 5 UI fixes requested before Phase 3: (1) Undo doesn't restore removed combos, (2) Orange highlight needs bold + fill, (3) Auto-scaling text size, (4) Background color too white, (5) Approximate match formatting.
- **Decided/Built:** All 5 fixes implemented: (1) ResultsPanel now has save_results_snapshot() / restore_results_snapshot() stack — FindTab saves before finalization, restores on undo. Results fully restored including previously removed combos. (2) Orange highlight in source_panel.py now uses bold font (QFont.setBold) + solid orange background (#FFE0B2) + dark orange text (#BF360C) — all three together for maximum visibility. (3) Font scaling via compute_font_scale() in styles.py — computes scale factor from screen height (reference: 1.0x at 1080p, clamped 0.80-1.30). All font-size values in all 6 GUI files now use scaled_size(N) instead of hardcoded Npx. (4) Background changed from pure white (#FFFFFF) to warm off-white cream (#FDFCF9 panels, #F7F5F0 main background). (5) Results format changed from "= sum [diff] (val + val)" to numbered "1. val, val = sum (diff)" — numbered within each size group, comma-separated values, sum after equals, difference in parentheses. Updated main.py to call compute_font_scale() before get_stylesheet(). 15 tests pass (12 original + 3 new: undo restores combos, combo format verification, font scaling).
- **Open:** Awaiting user testing of UI fixes before proceeding to Phase 3 (Excel Integration).

### 22-03-2026 | Session 2 | Message 9
- **Discussed:** UI comfort fixes — interface straining on eyes. Font too small (8-9pt visible), backgrounds too harsh, spacing too tight, list items too close together. Need accounting-app feel for articles/interns staring at it all day.
- **Decided/Built:** Comprehensive UI comfort pass across 8 files: (1) styles.py — base font 13→15px, tab padding 8→10px, button padding 7→10px, list item padding 4→7px vertical, input field padding 5→7px, GroupBox margin/padding increased, splitter handle 2→3px, text inputs keep #FFFFFF (only place pure white is used), all other panels use warm off-white. Active tab bg changed from #FFFFFF to #FDFCF9. (2) input_panel.py — margins 4→12, spacing 6→10, form spacing 6→10, small labels 11→13px. (3) results_panel.py — margins 4→12, spacing 4→8, title 14→17px, section headers 12→14px, size-group header font 10→13pt, Finalize button 13→15px. (4) source_panel.py — margins 4→12, spacing 4→8, total label 11→13px. (5) find_tab.py — margins 4→8. (6) summary_tab.py — margins 8→12, spacing 8→10, title 16→18px, stats 12→14px, cards spacing 6→10, card margins 8/6→12/10, card fonts all bumped 2px. (7) combo_info_panel.py — spacing 4→8, values label 12→14px. (8) main_window.py — placeholder titles 18→20px, desc 13→15px. All 15 tests pass.
- **Open:** Awaiting user testing of UI comfort fixes before proceeding to Phase 3.

### 22-03-2026 | Session 3 | Message 1
- **Discussed:** User confirmed UI fixes. Proceeded to Phase 3, Sub-phase 3A (Excel Reader + Settings Tab).
- **Decided/Built:** Sub-phase 3A complete — built 3 new files + updated 4: (1) readers/excel_reader.py (NEW) — ExcelHandler class: COM wrapper via pywin32 (win32com.client.GetActiveObject), connect/disconnect, get_workbooks/get_sheets, activate_sheet, read_selection with all production-hardened patterns: single-cell SpecialCells bypass, SpecialCells(12) for filtered/visible cells only, bulk read (area.Value) with cell-by-cell fallback, string thousand-separator stripping, _column_letter() conversion, _parse_cell_value() with Indian format support. Graceful degradation if pywin32 not installed (PYWIN32_AVAILABLE flag). (2) readers/excel_workbook_manager.py (NEW) — WorkbookManager class: tracks {workbook: {sheet: checked}} selections, refresh_workbooks() preserves check state, set_sheet_checked/set_workbook_checked, get_checked_sources(), grab_from_checked() loops through all checked sources with accumulative indexing, collects items+errors+warnings across sheets. (3) gui/settings_tab.py (NEW) — SettingsTab with: connection group (Connect/Disconnect buttons, status indicator), workbook/sheet group (QTreeWidget with checkboxes at both levels, toggling workbook toggles all sheets, tri-state parent, Refresh button, checked count label, hint text), color palette group (10x2 grid of color swatches with tooltips), About group. Signals: excel_connected, excel_disconnected. check_connection_alive() for monitor. (4) gui/input_panel.py (UPDATED) — Added "Grab from Excel" button with tooltip, grab_excel_requested signal, set_load_status() public method. (5) gui/source_panel.py (UPDATED) — Added add_items() for accumulative grab (extends existing list instead of replacing). (6) gui/find_tab.py (UPDATED) — Added set_workbook_manager(), _on_grab_from_excel() handler: validates connection → calls grab_from_checked(start_index) → add_items to source panel → shows status/warnings. (7) gui/main_window.py (UPDATED) — Creates ExcelHandler + WorkbookManager, replaces Settings placeholder with real SettingsTab, passes WorkbookManager to FindTab, wires excel_connected/disconnected signals, adds QTimer-based Excel monitor (EXCEL_MONITOR_INTERVAL=5s). 84 tests pass + 15 regression tests pass.
- **Open:** Awaiting user testing of Sub-phase 3A (Excel connection + grab) before proceeding to 3B (Excel Highlighter + Monitor + Close Handling).

### 22-03-2026 | Session 3 | Message 2
- **Discussed:** User reported 8 issues during Sub-phase 3A testing (issue 7 was OK — single cell works). All 7 remaining issues fixed.
- **Decided/Built:** 7 fixes across 6 files: (1) settings_tab.py — Removed Qt.ItemIsAutoTristate from workbook items, added early return for PartiallyChecked state in handler, fixed UnboundLocalError for `is_checked` in sheet branch. Individual sheets now independently checkable. (2) source_panel.py — Added grouped display mode: items with SourceTags show `[Book1.xlsx → Sheet1]` headers, cell references in item text (e.g., `A1: 245.70`). Items without SourceTag show flat (manual input). Header items use ITEM_INDEX_ROLE=-1 sentinel, skipped by highlight_combination(). (3) find_tab.py — Duplicate detection before adding items: builds set of (workbook, sheet, cell) from existing items, skips matches, shows "X duplicate cell(s) skipped" warning. Multi-column Proceed/Cancel dialog shown before loading multi-column data. Robust Excel connection check with try/except around is_connected. Replaced blocking wait(2000) with non-blocking _disconnect_solver_signals() + immediate UI state reset. Stop button disables immediately on click for visual feedback. (4) solver_manager.py — Progress signal throttling: on_progress() only emits every PROGRESS_SIGNAL_MIN_INTERVAL (0.25s) to prevent UI flooding on large searches. (5) constants.py — PROGRESS_CHECK_INTERVAL increased from 100 to 1000 (reduces callback overhead), added PROGRESS_SIGNAL_MIN_INTERVAL = 0.25. (6) dialogs.py — Added confirm_multi_column() dialog with Yes/No choice. New test file tests/test_fixes_3a.py with 37 tests. All 136 tests pass (84 + 15 + 37).
- **Open:** Awaiting user testing of all fixes before proceeding to Sub-phase 3B.

### 22-03-2026 | Session 3 | Message 3
- **Discussed:** User reported 6 additional issues during testing: (1) Orange background fill STILL not showing on selected combo items — QSS overrides QBrush, (2) Excel disconnect not detected after closing Excel, (3) Collapsible headers not working in results panel, (4) Summary tab doesn't show source info per number, (5) Need sticky sheet headers in source list, (6) Long sheet names need text wrapping.
- **Decided/Built:** 6 fixes across 4 files: (1) source_panel.py — Created SourceItemDelegate(QStyledItemDelegate) custom delegate that paints backgrounds directly in paint(), bypassing QSS entirely. Also added ScrollbarMarkerOverlay(QWidget) for position markers. Also added sticky header QLabel + setWordWrap(True). (2) main_window.py — Updated _check_excel_connection() to show dialog on disconnect. (3) results_panel.py — Added IS_COLLAPSED_ROLE, ▼/▶ indicators, _on_list_clicked() for collapse/expand. (4) summary_tab.py — Source info per value in cards. 45 tests pass.
- **Open:** User found scrollbar markers misaligned and UI lag during search.

### 22-03-2026 | Session 3 | Message 4
- **Discussed:** User reported 2 remaining issues: (1) Scrollbar markers not aligned with actual highlighted items — positions staggered and wrong. (2) UI lag during search — app not fully responsive while solver runs.
- **Decided/Built:** Root causes identified and fixed across 3 files: (1) source_panel.py — Scrollbar markers were using NumberItem.index / total_items for position calculation, but with grouped headers the list widget has more rows than items. Changed _update_scrollbar_markers() to scan actual list widget rows and compute row_fraction = row / total_rows. Added set_highlighted_positions_direct() method. Made markers wider (14px) and thicker (dynamic height) with bright orange + border for contrast. Also added _items_by_index dict for O(1) lookup in _style_list_item() — previously O(n) per item. setUpdatesEnabled(False/True) added around _rebuild_list(). (2) results_panel.py — The core lag cause: add_results() called _rebuild_list() which cleared and rebuilt the ENTIRE list from scratch on every batch. With 200+ accumulated combos, each batch arrival triggered O(n) clear + O(n) rebuild. Replaced with _add_combos_incremental() which inserts new items into existing size-group sections without clearing. Finds existing headers, appends after them, updates header counts. New size groups inserted in sorted position. Collapsed sections keep new items hidden. setUpdatesEnabled(False/True) wraps both incremental and full rebuild methods. (3) constants.py — BATCH_SIZE reduced from 20 to 10. New test file tests/test_fixes_3a_batch3.py with 36 tests. All 118 tests pass (37 + 45 + 36).
- **Open:** User approved. Proceeded to Sub-phase 3B.

### 22-03-2026 | Session 3 | Message 5
- **Discussed:** Sub-phase 3B: Excel Highlighter + Monitor + Close Handling.
- **Decided/Built:** Sub-phase 3B complete — built 2 new files + updated 2: (1) writers/excel_highlighter.py (NEW) — ExcelHighlighter class: highlight_combination() colors Excel cells via COM (Interior.Color using BGR format) and writes Shift+F2 notes (AddComment) on first cell with combo number, label, sum, difference, and cross-workbook/sheet references. remove_highlight() clears colors and comments for undo. highlight_unmatched() marks remaining cells grey (for Phase 4B). _rgb_to_excel() converts RGB→BGR. _build_note_text() formats multi-line note. All methods return {"success", "error", "data"} format. Graceful degradation when not connected. (2) readers/excel_monitor.py (NEW) — ExcelMonitor class: tracks open workbook file paths via update_workbook_paths(), offers reopen_workbooks() using os.startfile(), clear() on disconnect. (3) gui/main_window.py (UPDATED) — Creates ExcelHighlighter and ExcelMonitor in _setup_ui(). Passes highlighter to FindTab. Renamed timer to _excel_monitor_timer. _on_excel_connected() now calls monitor.update_workbook_paths(). _check_excel_connection() now offers to reopen workbooks via warn_excel_closed dialog. closeEvent() enhanced: checks for finalized combos, shows confirm_close_unsaved dialog (Save/Discard/Cancel), Cancel aborts close, always stops timer and disconnects Excel cleanly. (4) gui/find_tab.py (UPDATED) — Added set_excel_highlighter(). _on_finalize_requested() now calls highlighter.highlight_combination() immediately after finalization (non-fatal on error). undo_last_finalization() now calls highlighter.remove_highlight(). Key architectural decision: highlighting happens immediately on finalization (not deferred to close) so users see colored cells as they work. New test file tests/test_phase_3b.py with 76 tests. All 194 tests pass (37 + 45 + 36 + 76).
- **Open:** Awaiting user testing of Sub-phase 3B (highlight cells, Shift+F2 notes, Excel monitor reopen, close event) before proceeding to Phase 4.

### 22-03-2026 | Session 4 | Message 1
- **Discussed:** User reported 5 issues from Sub-phase 3B testing: (1) Shift+F2 note only on first cell, not all cells. (2) Undo works correctly — confirmed OK. (3) Excel close detection not reliable — timer/COM ping not catching Excel closure. (4) Reopen dialog not appearing when Excel close IS detected. (5) Smart bounds hints (viable min/max sizes) not showing in the UI.
- **Decided/Built:** 4 fixes across 5 files: (1) excel_highlighter.py — Moved _write_cell_note() inside the for loop so EVERY Excel-sourced cell gets the Shift+F2 note, not just the first. Note text built once before the loop for efficiency. Return data changed from "note_written" (bool) to "notes_written" (int). (2) excel_reader.py — Enhanced is_connected property with a two-step COM liveness check: tries _excel_app.Name (tighter check) before Workbooks.Count. Both must succeed. Name check catches cases where COM proxy is stale but Count still works briefly after Excel closes. (3) main_window.py — Wrapped _check_excel_connection() in try/except so any stray COM exception during the check doesn't crash the timer silently. (4) excel_monitor.py — Fixed update_workbook_paths() to always store workbook names (for display in reopen dialog) even for unsaved workbooks. Previously only stored names when os.path.isfile(FullName) was True, which excluded unsaved workbooks like "Book1" and prevented the reopen dialog from ever appearing. (5) input_panel.py + find_tab.py — Added target_changed and buffer_changed signals. FindTab._update_bounds_hint() computes smart bounds live when numbers are loaded or target/buffer changes. Called from _on_numbers_loaded(), _on_grab_from_excel(), and via signal connections. Silently clears hint if target is empty or parameters are invalid. New test file tests/test_fixes_3b_batch1.py with 48 tests. All 169 tests pass (76 + 45 + 48).
- **Open:** Awaiting user re-testing of all 4 fixes before proceeding to Phase 4.

### 22-03-2026 | Session 4 | Message 2
- **Discussed:** Three items before testing 3B fixes: (1) Skip zeros from all number sources, (2) Create reference solver verification test suite, (3) Reorder remaining phases.
- **Decided/Built:** (1) Zero-skip rule: Added `if value == 0.0: continue` in core/number_parser.py (both line-separated and semicolon-separated parsers) and readers/excel_reader.py (_read_single_cell and _process_cell_list). Zeros silently excluded — they don't contribute to sums. (2) Created tests/test_solver_verification.py — 32 test sections, 78 assertions covering: basic exact, approx with buffer, negatives, seeds, search order, stop flag, max results (exact unlimited / approx capped), smart bounds, edge cases (empty, single, all equal, target zero, all negatives, zeros excluded), large dataset (500+ items with mathematical verification), floating point precision, determinism. C solver (Phase 6) must pass all 78 identically. (3) Reordered remaining phases: 4A (Seeds) → 6 (C Solver, since solver interface is now final) → 4B (Unmatched) → 4C (Session) → 5 (Reports) → 7 (Nice-to-haves).
- **Open:** Awaiting user go-ahead after testing 3B fixes before proceeding to Phase 4A.

### 23-03-2026 | Session 5 | Message 1
- **Discussed:** Phase 4A: Seed Numbers (N2).
- **Decided/Built:** Phase 4A complete — seed UI added across 3 files: (1) source_panel.py — Double-click to toggle seed pin/unpin on items. Finalized items cannot be pinned. Seeds shown with [SEED] prefix (already existed), light blue background (#BBDEFB), bold dark blue text (#0D47A1). Header shows seed count ("5 numbers (2 pinned)"). New public methods: get_seed_indices(), get_seed_count(), clear_all_seeds(). New signal: seeds_changed. (2) find_tab.py — Collects seed_indices from source_panel and passes to validate_search_parameters() in both _on_find_requested() and _update_bounds_hint(). Connected seeds_changed signal to _on_seeds_changed() which updates seed info display and recomputes bounds. Clear All now calls clear_all_seeds(). (3) input_panel.py — Added seed info label showing "N seeds pinned (sum: X)" in the search parameters group. New methods: update_seed_info(), clear_seed_info(). Cleared on Clear All. Backend was already complete: NumberItem.is_seed field, SearchParameters.seed_indices, solver_python.py seed handling, parameter_validator.py seed_indices passthrough. New test file tests/test_phase4a.py: 15 test sections, 45/45 passing. All 13 existing test suites pass (zero regressions).
- **Open:** Awaiting user testing of Phase 4A before proceeding to Phase 6 (C Solver).

### 23-03-2026 | Session 5 | Message 2
- **Discussed:** Two fixes before Phase 6: (1) Smart bounds hints still not showing — reported twice. (2) No way to deselect a combo.
- **Decided/Built:** 2 fixes across 3 files: (1) input_panel.py — Smart bounds hints now show INLINE next to the Min Size and Max Size spinboxes ("← viable: 2", "← viable: 5") in bold blue text, impossible to miss regardless of window size. Summary label below still shows "Viable: 2 to 5 (1,200 combinations)". No-solution shows red "No solution" inline. Previous approach had a single label buried at the bottom of the form layout that was easy to miss. (2) results_panel.py — Added "Deselect" button next to "Finalize Selected". Clicking it clears selection in both lists, disables both buttons, and emits deselection_requested signal. FindTab connects this to clear orange highlights from source panel via highlight_combination(None). Also fixed _on_exact_item_changed to properly disable Finalize when deselected (was missing None check). All 13 existing test suites pass (zero regressions).
- **Open:** Proceeding to Phase 6 (C Solver).

### 23-03-2026 | Session 6 | Message 1
- **Discussed:** Phase 6: C Solver DLL — build, wrap, integrate, verify.
- **Decided/Built:** Phase 6 complete — high-performance C solver via ctypes: (1) csolver/solver.h — Public API header: CombinationResult struct (indices[512], count, sum_value, difference), ProgressCallback typedef (returns 0=continue, non-zero=stop), find_combinations_c() declaration with SOLVER_API __declspec(dllexport). (2) csolver/solver.c — Iterative combination generation matching Python itertools.combinations() lexicographic order. _next_combination() advances index array, _compute_sum() sums values, _round2() rounds to 2dp. Separates seeds from search items via seed_flags array, adjusts target by subtracting seed sum. Exact matches NEVER capped, approx capped at max_results. Progress callback checked every 1000 iterations AND after every result found. (3) csolver/build.bat — Compiles with `gcc -shared -O2 -o ..\solver.dll solver.c -lm`. Compiled successfully with GCC 15.2.0 (MinGW-W64), producing 48KB solver.dll. (4) core/solver_c.py — ctypes wrapper: load_solver_dll() loads DLL and configures argtypes/restype, find_combinations_c() converts NumberItem lists to C arrays, bridges Python stop_flag + progress_callback into C callback, calls DLL, converts CombinationResult array back to Combination objects. (5) core/solver_manager.py — SolverThread updated with _run_c_solver() method (same batching/signal logic as Python). SolverManager._load_dll() replaces _check_dll(). create_solver_thread() now sets `use_c = self._dll_available` and passes `c_dll=self._c_dll`. (6) tests/test_c_solver_verification.py — Runs same 78-assertion suite against C solver via monkey-patching. TEST 9 (Stop Flag) adapted for C architecture: Python test counted results via generator yield (doesn't work with batch C call), replaced with immediate-stop test (lambda: True → 0 results). **Result: 78/78 passed.** C solver produces identical results to Python solver.
- **Assumption:** Stop flag test adapted because C solver runs in one batch call (not a Python generator). The C solver checks stop flag via progress callback every 1000 iterations and after every result — this works correctly in production (SolverThread._should_stop()), but the Python test's result-counting mechanism only works with generators. This is an architectural difference, not a bug.
- **Open:** Phase 6 complete. Next: Phase 4B (Unmatched Numbers View). Awaiting user testing.

### 23-03-2026 | Session 6 | Message 2
- **Discussed:** Two C solver bugs reported during testing: (1) No live streaming — C solver collected all results in a buffer and dumped them at once on completion/stop, making the UI appear frozen during search. (2) Integer overflow — combination counter wrapped to 0 at ~2.1 billion iterations, making the app look like it restarted the search.
- **Decided/Built:** 2 fixes across 4 files: (1) **Live streaming**: Added `ResultCallback` typedef to solver.h/solver.c — each valid combination is delivered immediately via callback instead of stored in a buffer. `_emit_result()` helper handles both modes: streaming (result_cb non-NULL) or batch (result_cb NULL, for test suite compatibility). In solver_c.py, split into two functions: `find_combinations_c()` (batch generator for tests) and `find_combinations_c_streaming()` (regular function for live UI). Key insight: Python generator functions don't execute until iterated, so streaming code can't live inside a generator — had to be a separate non-generator function. solver_manager.py `_run_c_solver()` now uses streaming with `on_result` callback that does real-time batching and signal emission. (2) **64-bit overflow**: Root cause was `pyqtSignal(int, int)` which maps to C++ `int` (32-bit on Windows), truncating iteration counts above 2^31. Fixed by changing to `pyqtSignal(object, int)` — passes iteration count as Python object (unlimited precision). Also changed C counters `result_count` and `approx_found` from `int32_t` → `int64_t`, and function return type from `int32_t` → `int64_t` with matching ctypes `restype = c_int64`. All tests pass: 78/78 Python, 78/78 C solver, all phase suites.
- **Open:** Phase 6 fully complete with streaming and overflow fixes. Next: Phase 4B (Unmatched Numbers View).

### 23-03-2026 | Session 6 | Message 3
- **Discussed:** Two UI issues: (1) Smart bounds hints working backwards — gated on full parameter validation instead of showing as guidance. (2) No solver indicator — user can't tell if C or Python solver is active.
- **Decided/Built:** 2 fixes across 2 files: (1) **Smart bounds as guidance**: find_tab.py `_update_bounds_hint()` rewritten to only need a parsed target and buffer (via `parse_target()`/`parse_buffer()` directly), not full `validate_search_parameters()`. Computes bounds over the full possible range (1 to item_count) regardless of what min/max size the user has set. Hints now show immediately when numbers are loaded and target is entered — no longer blocked by min>max or other validation errors. (2) **Solver indicator**: Added `_solver_label` to input_panel.py below the Find/Stop buttons. Shows "Solver: C (solver.dll)" in green when DLL is loaded, or "Solver: Python (fallback)" in grey. Updated on app startup and before each search (in case DLL was added/removed between searches). All tests pass.
- **Open:** Awaiting user testing of smart bounds guidance + solver indicator.

### 23-03-2026 | Session 6 | Message 4
- **Discussed:** Combo info panel pushes source list off-screen when large combo selected. Viable bounds text and combination count overflow the input panel width with very large numbers.
- **Decided/Built:** 2 UI fixes: (1) **Combo info panel scroll**: combo_info_panel.py — wrapped item values in a `QScrollArea` with `setMaximumHeight(150)`. Summary line (sum, diff, count) stays above the scroll area, always visible. Scroll area hidden when no combo selected. Large combos now scroll within a fixed area instead of expanding indefinitely. (2) **Label overflow**: input_panel.py — added `setWordWrap(True)` and `setMinimumWidth(0)` on all labels that could overflow: `_min_hint_label`, `_max_hint_label`, `_bounds_hint_label`, `_seed_info_label`, `_progress_label`. Set `QFormLayout.setFieldGrowthPolicy(AllNonFixedFieldsGrow)` so fields fill available width instead of pushing the layout wider. Text now wraps within the panel boundaries.
- **Open:** Awaiting user testing.

### 23-03-2026 | Session 6 | Message 5
- **Discussed:** Phase 4B — Unmatched Numbers View. User said "Proceed to Phase 4B." Per the build plan: "Finalize some combos → click 'Mark Unmatched' → remaining Excel cells turn grey → finalize another combo from unmatched → grey replaced by match color."
- **Decided/Built:** 3 files modified: (1) **summary_tab.py** — Added `mark_unmatched_requested` signal, "Mark Unmatched" button (grey, positioned left of Undo), `show_unmatched_result()` method that updates button text after highlighting. `refresh()` now accepts `unmatched_count` and `total_items` params; stats bar shows unmatched count. Button enabled only when finalized combos exist AND unmatched items remain. (2) **main_window.py** — Wired `mark_unmatched_requested` signal to new `_on_mark_unmatched()` handler. Gets unmatched items via `FinalizationManager.get_unmatched_items()`, calls `ExcelHighlighter.highlight_unmatched()` with `UNMATCHED_COLOR` grey RGB. `_on_finalization_changed()` now passes unmatched/total counts to SummaryTab. (3) **find_tab.py** — Added `get_all_items()` accessor so MainWindow can retrieve the full items list. Backend already existed: `get_unmatched_items()`, `highlight_unmatched()`, `UNMATCHED_COLOR = ((220, 220, 220), "Light Grey")`.
- **Assumption:** When a previously-grey unmatched cell gets finalized in a later combo, the finalization color overwrites the grey automatically (ExcelHighlighter.highlight_combination sets cell color regardless of current color). No special "un-grey" logic needed.
- **Open:** Phase 4B complete. Ready for user testing.

### 23-03-2026 | Session 6 | Message 6
- **Discussed:** Phase 4C — Session Save/Restore. User said "Proceed to Phase 4C." Per the build plan: auto-save every 60 seconds AND immediately after every finalization or undo. Crash recovery via session file. Restore prompt on startup.
- **Decided/Built:** 6 files modified/created:
  1. **core/session_manager.py** (NEW) — `SessionManager` class with static methods: `save_session()` serializes NumberItems, FinalizedCombinations, and search params to `userdata/session.json` using atomic write (temp file + `os.replace`). `load_session()` deserializes back to live objects. `get_session_summary()` reads timestamp/counts for the restore dialog without full deserialization. `has_saved_session()` and `delete_session()` for lifecycle management. Full roundtrip test passed: values, source tags, finalization colors, seeds, search params all survive serialization.
  2. **gui/dialogs.py** — Added `ask_session_restore()` dialog with three buttons: "Restore" (load session), "Discard" (delete file, start fresh), "Start Fresh" (keep file, do nothing). Shows saved timestamp, item count, and finalized count.
  3. **gui/input_panel.py** — Added `get_search_params()` and `set_search_params()` for serializing/restoring target, buffer, min/max size, max results, and search order.
  4. **gui/find_tab.py** — Added `get_session_state()` (collects all state for save) and `restore_session()` (loads items, restores finalization state, sets search params, refreshes display).
  5. **gui/main_window.py** — Added auto-save QTimer (`SESSION_AUTOSAVE_INTERVAL` = 60s). `_on_finalization_changed()` now calls `_save_session()` immediately. `closeEvent()` saves on "save", deletes file on "discard", silently saves items-only sessions. `try_restore_session()` called from main.py after `window.show()`.
  6. **main.py** — Added `window.try_restore_session()` call after `show()`.
- **Assumptions:** (1) Undo stack is NOT saved across sessions — restoring a session implicitly accepts its state, undo starts fresh. (2) Atomic write via temp file + `os.replace` prevents corrupted saves if app crashes mid-write. (3) Sessions with 0 items and 0 finalizations are auto-deleted (no restore prompt for empty sessions). (4) Auto-save timer runs continuously; save is a no-op if nothing is loaded.
- **Open:** Phase 4C complete. Ready for user testing.
